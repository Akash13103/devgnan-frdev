import React, { Component } from 'react';
import SideBar from '../SideBar/SideBar';

class Bids extends Component {
    render() {
        return (
            <div>
                <SideBar/>
                Bids
            </div>
        );
    }
}

export default Bids;
