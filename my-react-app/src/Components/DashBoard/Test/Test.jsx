import React from "react";

const Test = () => {
  return <div></div>;
};

export default Test;
//Redux Toolkit is a global state management it is also called as  predictable state container .it is to overcome the problem of context api
// redux slices==>loginform slice==>store.js==>main.jsx provider adding==> useSelector()
